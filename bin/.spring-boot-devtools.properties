# Spring Boot Configuration Property Suppressions
# This file tells Spring Boot IDE to ignore warnings for custom properties

# Suppress warnings for custom JWT properties
spring.ide.config.ignore=app.jwt.secret,app.jwt.expiration-time,app.jwt.issuer,app.jwt.token-prefix,app.jwt.header-name
